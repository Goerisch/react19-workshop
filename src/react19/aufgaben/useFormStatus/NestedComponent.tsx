export const NestedComponent = () => {
    return (
        <div>
            {/*
            {pending && <div>Cart is updating...</div>}
            <p>Adding Item with ID:{data?.get(FormNames.id)?.toString()}</p>
            <p>Quantity: {data?.get(FormNames.quantity)?.toString()}</p>*/}
        </div>
    );
};
